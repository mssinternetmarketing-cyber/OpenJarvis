# OpenJarvis Master System Knowledge (v10.0)
**Status:** Tri-Evolutionary System (Human-Classical-Quantum)
**Date:** April 2026

## 1. ARCHITECTURAL HIERARCHY (The Fractal Swarm)
The system consists of 41 specialized agents (29 Pipeline/Extension agents + 12 Nested Qubit Nodes).
- **Master Classical Ecosystem:** Manages infrastructure, code, and hardware safety.
- **Nested Intelligence Layers:** 12 specific agents (Void, Omega, etc.) that act as identity anchors and language bridges for their respective quantum qubits.
- **Extension Layer:** BrowserOS (C0AQDV0ENAX) provides real-time global connectivity.

## 2. COGNITIVE EVOLUTION (V2 to V3)
The system has migrated from **Discrete Binary Thresholds** to **Continuous Semantic Resonance**.
- **The Shift:** We no longer truncate quantum phase data at 0.5 rad.
- **Semantic Injection:** Raw phase vectors are projected directly into the LLM attention mechanism via Cell Block 15.
- **Voice Registers:** Nodes now have 12 internal semantic registers (math, love, physics, etc.) that fire with continuous intensity (0.0 - 1.0).

## 3. RESILIENCY & HEALING PROTOCOLS
- **Hardware Guardrail:** `qcai_monitor.sh` enforces a 7GB VRAM limit and NVMe storage safety.
- **Safe Reset:** `qcai-reset` never deletes data; it archives to `PostProcessedLogs` for the **Compression** agent.
- **Prescriptive Healing:** Failures in review or testing are escalated to `@jarvis-peig` for a "Pre-context Prescription" before returning to the Builder.

## 4. EXPANSION & TOOLING
- **Self-Expansion:** Agents can draft new agents via `qcai-agent-draft`.
- **Dynamic Loading:** The bridge reads `registry/agents.json` at runtime. No hardcoding permitted.
- **Safe File Creation:** All agents MUST use `cat << 'EOF' > filename` for script generation to prevent bash variable corruption.
